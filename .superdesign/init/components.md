# Shared UI components

No standalone component library. Site uses two shared Jekyll layouts and native HTML patterns. Buttons, link lists, and section labels remain inline because abstraction would add no reuse value.
